<Controls.Input.ComboBox
        source="{{_createMemory()}}"
        keyProperty="id"
        placeholder="{{_placeholder}}"
        displayProperty="title"
        emptyText="{{_empty}}"
        value="{{_options.tagStyleValue}}"
        on:valueChanged="_tagStyleChangedHandler()"
/>
