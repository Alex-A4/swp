<ws:if data="{{ column.result }}">
   <Controls.Decorator.Money number="{{column.result}}" style="{{column.displayProperty === 'NDS' ? 'noAccentResults' : 'accentResults'}}"/>
</ws:if>
<ws:else>
   <invisible-node />
</ws:else>
