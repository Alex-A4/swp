<ws:partial template="{{_options.content}}"
            value="{{_value}}"
            on:valueChanged="_valueChanged()"
            on:searchClick="_searchClick()"
            on:keyDown="_keyDown()"/>