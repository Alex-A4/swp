<Controls.Button
   viewMode="link"
   style="secondary"
   attr:title="{{_options.title}}"
   icon="icon-{{_options.icon}} icon-medium {{_readOnly ? 'icon-disabled' : 'icon-primary'}}"
   readOnly="{{_readOnly}}"
   on:click="_clickHandler()"/>
