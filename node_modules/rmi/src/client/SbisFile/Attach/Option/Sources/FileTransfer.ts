/// <amd-module name="SbisFile/Attach/Option/Sources/FileTransfer" />

import SourceOption = require("File/Attach/Option/Source");
import LocalFile = require("File/LocalFile");

const LINK = "SbisFile/Source/FileTransfer";

/**
 * Класс конфигурации ISource для загрузки на FileTransfer
 * Позволяет загружать ресурсы сущности {@link File/LocalFile}
 * @class
 * @name SbisFile/Attach/Option/Sources/FileTransfer
 * @extends File/Attach/Option/Source
 * @public
 * @author Заляев А.В
 */
class FileTransfer extends SourceOption {
    /**
     * @cfg {String} storage Используемое хранилище
     * @see {@link https://online.sbis.ru/shared/disk/862e8a62-ff97-41b0-8311-29d4f87d1f3d}
     */
    /**
     * @cfg {String} serviceUrl Адрес сервиса FileTransfer
     */
    /**
     * @param {Object} [options] Параметры вызова конструктора
     * @constructor
     * @see File/LocalFile
     * @see WS.Data/Source/ISource
     */
    constructor (options?: any) {
        super (LINK, LocalFile,  options || {});
    }
}
export = FileTransfer;
