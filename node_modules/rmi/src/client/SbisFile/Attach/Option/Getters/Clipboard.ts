/// <amd-module name="SbisFile/Attach/Option/Getters/Clipboard" />
import ResourceGetter = require("File/Attach/Option/GetterLazy");

const GETTER_LINK = "SBIS3.Plugin/Integration/ResourceGetter/Clipboard";
const GETTER_TYPE = "ClipboardGetter";

/**
 * Класс конфигурации {@link File/IResourceGetter} для получения файлов из буфера обмена,
 * передаваемый в {@link File/ResourceGetter/Attach}
 * @class
 * @name SbisFile/Attach/Option/Getters/Clipboard
 * @extends File/Attach/Option/GetterLazy
 * @public
 * @author Заляев А.В.
 */
class Clipboard extends ResourceGetter {
    /**
     * @param {*} [options] Параметры вызова конструктора
     * @constructor
     * @see File/IResourceGetter
     */
    constructor (options?: any) {
        super (GETTER_LINK, GETTER_TYPE, options || {});
    }
    /**
     * @description
     * Тип геттера передается в метод choose File/Attach, чтобы указать каким способом(геттером) выбираются файлы
     * <pre>
     *   attach.choose(Clipboard.getType()); // Получение файлов из буфера обмена
     * </pre>
     * @static
     * @method
     * @returns {String} Тип Getter'a
     */
    static getType() {
        return GETTER_TYPE;
    }
}
export = Clipboard;
