/// <amd-module name="SbisFile/Attach/Option/Getters/Dialogs" />
import ResourceGetter = require("File/Attach/Option/GetterLazy");

const GETTER_LINK = "SBIS3.Plugin/Integration/ResourceGetter/Dialogs";
const GETTER_TYPE = "DialogsGetter";

/**
 * Класс конфигурации {@link File/IResourceGetter} для получения файлов из диалогового окна СБИС Плагина,
 * передаваемый в {@link File/ResourceGetter/Attach}
 * @class
 * @name SbisFile/Attach/Option/Getters/Dialogs
 * @extends File/Attach/Option/GetterLazy
 * @public
 * @author Заляев А.В.
 */
class Dialogs extends ResourceGetter {
    /**
     * @param {*} [options] Параметры вызова конструктора
     * @constructor
     * @see File/IResourceGetter
     */
    constructor (options?: any) {
        super (GETTER_LINK, GETTER_TYPE, options || {});
    }
    /**
     * @description
     * Тип геттера передается в метод choose File/Attach, чтобы указать каким способом(геттером) выбираются файлы
     * <pre>
     *   attach.choose(Dialogs.getType()); // Получение через окно выбора СБИС Плагин
     * </pre>
     * @static
     * @method
     * @returns {String} Тип Getter'a
     */
    static getType() {
        return GETTER_TYPE;
    }
}
export = Dialogs;
