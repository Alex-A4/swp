define('Core/core-structure', ['Core/Deferred','Core/constants', 'Core/ConsoleLogger'], function( Deferred, constants, ConsoleLogger) {
   if (typeof window !== 'undefined') {
      return {};
   }

   var global = (function(){ return this || (0,eval)('this'); }());
   if (!global.$ws) {
      global.$ws = {
         proto: {
            Deferred: Deferred, //require('Core/Deferred')
            ConsoleLogger: ConsoleLogger
         },
         _const: constants //require('Core/constants')
      };
   }
   return global.$ws;
});
