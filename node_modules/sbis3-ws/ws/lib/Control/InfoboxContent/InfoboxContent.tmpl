<div>
   <div class="ws-infobox-title">
      <ws:partial if="{{title}}" template="{{title}}" scope="{{options}}"/>
   </div>
   <div class="ws-infobox-content">
      <ws:partial if="{{message}}" template="{{message}}" scope="{{options}}"/>
   </div>
</div>
