declare type WSStorageNative = {
    getItem(key: string, noCheckSession?:boolean): string;
    removeItem(key: string);
    setItem(key: string, item: string, noCheckSession?: boolean): boolean;
    clear(noCheckSession?: boolean);
    getKeys(): Array<string>
}
declare type WSStorage = {
    new(prefix?: string, storeAlways?: boolean, isCheckSession?: boolean)
    getItem(key: string): any;
    getItem<T>(key: string): T;
    removeItem(key: string);
    setItem(key: string, item: any): boolean;
    clear();
    getKeys(): Array<string>
}
export {
    WSStorage,
    WSStorageNative
}
