/* global define */
define('WS.Data/Entity/Abstract', [
   'Data/type',
   'Core/core-extend'
], function(
   type,
   coreExtend
) {
   'use strict';

   // Deprecated
   var Abstract = coreExtend.extend(type.Abstract, {
      '[WS.Data/Entity/Abstract]': true
   });

   return Abstract;
});
