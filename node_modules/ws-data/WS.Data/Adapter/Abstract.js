/* global define */
define('WS.Data/Adapter/Abstract', [
   'Data/type',
   'Core/core-extend'
], function(
   type,
   extend
) {
   'use strict';

   return extend.extend(type.adapter.Abstract, {
   });
});
