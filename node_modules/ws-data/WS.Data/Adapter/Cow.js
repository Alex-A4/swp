/* global define */
define('WS.Data/Adapter/Cow', [
   'Data/type',
   'WS.Data/Di'
], function(
   type,
   Di
) {
   'use strict';

   Di.register('adapter.cow', type.adapter.Cow);

   return type.adapter.Cow;
});
