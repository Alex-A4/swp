/* global define */
define('WS.Data/Source/Rpc', [
   'Data/source',
   'Core/core-extend'
], function(
   source,
   extend
) {
   'use strict';


   var Rpc = extend.extend(source.Rpc, {
      '[WS.Data/Source/Rpc]': true
   });

   return Rpc;
});
